---
description: Aplica um dos 15 verbos de refino
argument-hint: "<verbo> [alvo]"
---

Verbos: bolder · quieter · distill · polish · delight · overdrive · typeset · layout · colorize · harden · onboard · clarify · adapt · optimize · extract

Sem verbo nomeado, invoque `Skill(refine-verbs)` e mapeie o pedido pela tabela de linguagem natural.

Despache o agente dono do verbo: `clarify` e `onboard` vão para `fc-ux-architect`; `optimize` começa em `fc-a11y-perf-auditor` para medir; `extract` vai para `fc-design-system`; os demais para `fc-implementer`.

Se o verbo pedido não ataca o problema real, diga isso antes de executar.

$ARGUMENTS
