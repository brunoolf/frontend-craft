---
description: Trabalho de motion - micro-interacoes, scroll, 3D
argument-hint: "[alvo]"
---

Despache `fc-motion-engineer`.

Se o pedido é vago sobre o que animar ("deixa mais vivo", "o que poderia animar aqui"), ele roda `motion-audit` primeiro — read-only — e propõe antes de implementar.

Se envolve scroll com cena de fundo ou 3D, ele carrega `scroll-experiences`. Confirme com o usuário o custo antes de trazer WebGL: bundle, bateria e complexidade.

Alvo: $ARGUMENTS
